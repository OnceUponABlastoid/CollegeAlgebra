\documentclass{ximera}
\title{Transformations of Graphs}

\outcome{Identify and describe transformations (shifts, reflections, stretches) from function notation.}
\outcome{Determine the effect of a transformation on the domain and on specific points.}
\outcome{Write an equation for a transformed function given the transformations in order.}
\outcome{Express one function as a transformation of another from their graphs.}

\begin{document}
\begin{abstract}
\end{abstract}
\maketitle

\textbf{Learning Objectives:}
\begin{itemize}
  \item Identify and describe transformations (shifts, reflections, stretches) from function notation.
  \item Determine the effect of a transformation on the domain and on specific points.
  \item Write an equation for a transformed function given the transformations in order.
  \item Express one function as a transformation of another from their graphs.
\end{itemize}
\medskip

\section*{Quick Check}

\begin{problem}
If the domain of $f(x)$ is $[-3,1]$, what is the domain of $g(x) = 2f(-x+1)$?
\begin{hint}
Solve the inequality $-3 \leq -x+1 \leq 1$ to find which $x$-values keep the argument of $f$ within its domain.
\end{hint}
\begin{multipleChoice}
  \choice{$[-2,2]$}
  \choice{$[-6,2]$}
  \choice{$[-1,3]$}
  \choice[correct]{$[0,4]$}
\end{multipleChoice}
\end{problem}

\begin{problem}
The function $g(x)$ passes through $(4,-1)$ and has a vertical asymptote at $x=1$. What can you say about $h(x) = 3g(-x-2)+1$?
\begin{hint}
To find where a known point $(a,b)$ on $g$ maps to on $h$: solve $-x-2 = a$ for the new $x$-coordinate, then compute $3b+1$ for the new $y$-coordinate.
\end{hint}
\begin{multipleChoice}
  \choice{Must pass through $(13,3)$.}
  \choice{Must have a vertical asymptote at $x=4$.}
  \choice{Must have a vertical asymptote at $y=-3$.}
  \choice[correct]{Must pass through $(-6,-2)$.}
\end{multipleChoice}
\end{problem}

\begin{problem}
The following transformations are applied in order to $f(x) = \sqrt{x}$: (1) shift left 3, (2) shift up 1, (3) reflect over the $y$-axis. Which is the resulting function $g(x)$?
\begin{hint}
Apply each transformation one at a time to the function expression.
\end{hint}
\begin{multipleChoice}
  \choice[correct]{$g(x) = \sqrt{-x+3}+1$}
  \choice{$g(x) = -\sqrt{x+3}-1$}
  \choice{$g(x) = -\sqrt{x+3}+1$}
  \choice{$g(x) = \sqrt{-x-3}+1$}
\end{multipleChoice}
\end{problem}

\begin{problem}
The graph of $y = f(x)$ passes through $(2,5)$. Which of the following must be a point on the graph of $y = f(x+3) - 2$?
\begin{hint}
If $(a,b)$ is on $f$, then $(a-3,\,b-2)$ is on $f(x+3)-2$.
\end{hint}
\begin{multipleChoice}
  \choice[correct]{$(-1,3)$}
  \choice{$(5,3)$}
  \choice{$(-1,7)$}
  \choice{$(5,7)$}
\end{multipleChoice}
\end{problem}

\begin{problem}
The graphs of $f(x)$ and $g(x)$ are shown below. Find an equation for $g(x)$ in terms of $f(x)$.

\begin{center}
\begin{tikzpicture}[scale=0.8]
\begin{axis}[
  axis lines=middle,
  xmin=-5, xmax=5,
  ymin=-3, ymax=4,
  xtick={-5,...,5},
  ytick={-3,...,4},
  grid=both,
  width=7cm, height=5cm,
  xlabel={$x$}, ylabel={$f(x)$}
]
\addplot[very thick, blue, domain=-4:-1] {x+3};
\addplot[very thick, blue, domain=-1:2] {2};
\addplot[very thick, blue, domain=2:4] {-x+4};
\addplot[only marks, mark=*, mark options={fill=white}, blue] coordinates {(-4,-1)};
\addplot[only marks, mark=*, blue] coordinates {(-1,2) (2,2) (4,0)};
\end{axis}
\end{tikzpicture}
\quad
\begin{tikzpicture}[scale=0.8]
\begin{axis}[
  axis lines=middle,
  xmin=-7, xmax=3,
  ymin=-3, ymax=4,
  xtick={-7,-6,...,3},
  ytick={-3,...,4},
  grid=both,
  width=7cm, height=5cm,
  xlabel={$x$}, ylabel={$g(x)$}
]
\addplot[very thick, red, domain=-6:-3] {-x-4};
\addplot[very thick, red, domain=-3:0] {-1};
\addplot[very thick, red, domain=0:2] {x-1};
\addplot[only marks, mark=*, mark options={fill=white}, red] coordinates {(-6,2)};
\addplot[only marks, mark=*, red] coordinates {(-3,-1) (0,-1) (2,1)};
\end{axis}
\end{tikzpicture}
\end{center}

\begin{hint}
Check how the domain and $y$-values of $g$ relate to those of $f$ — look for horizontal shifts and reflections.
\end{hint}
\begin{multipleChoice}
  \choice{$g(x) = f(-x-2)+1$}
  \choice{$g(x) = -f(x-2)-3$}
  \choice[correct]{$g(x) = -f(x+2)+1$}
\end{multipleChoice}
\end{problem}


\bigskip
\section*{Extended Practice}

\begin{problem}
Given $f(x)$, describe the transformation(s) needed to obtain each function.
\begin{enumerate}
  \item[(a)] $f(x+3)$
  \medskip
  \item[(b)] $2 - f(x)$
  \medskip
  \item[(c)] $f(3x)$
  \medskip
  \item[(d)] $f(-x-6)$
  \medskip
  \item[(e)] $f(x) + 3$
  \medskip
  \item[(f)] $3f(x)$
\end{enumerate}

\begin{solution}
\begin{enumerate}
  \item[(a)] Shift \textbf{left} 3 units.

  \item[(b)] Rewrite as $-f(x)+2$: (i) reflect over the $x$-axis, (ii) shift up 2 units.

  \item[(c)] Shrink \textbf{horizontally} by a factor of $\dfrac{1}{3}$.

  \item[(d)] Rewrite as $f(-(x+6))$: (i) shift right 6 units, (ii) reflect over the $y$-axis.

  \item[(e)] Shift \textbf{up} 3 units.

  \item[(f)] Stretch \textbf{vertically} by a factor of 3.
\end{enumerate}
\end{solution}
\end{problem}


\begin{problem}
For each function, (i) identify the parent function, (ii) list the transformations in order.
\begin{enumerate}
  \item[(a)] $m(x) = -\left|\dfrac{1}{3}x+1\right|$
  \medskip
  \item[(b)] $\ds g(x) = -\dfrac{1}{3}(x+2)^2 + 3$
\end{enumerate}

\begin{solution}
\begin{enumerate}
  \item[(a)] Parent: $f(x) = |x|$.\\
  Transformations: (i) shift left 1 unit, (ii) stretch horizontally by a factor of 3, (iii) reflect over the $x$-axis.

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=5cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-10.5, xmax=6.5, xtick={-10,-5,0,5},
  ymin=-5.5, ymax=5.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, magenta, domain=-5.5:5.5, samples=60] {abs(x)};
\addplot[only marks, mark=*, magenta] coordinates {(0,0) (-1,1) (1,1)};
\node[magenta, font=\small] at (axis cs:4,2.5) {$f(x)=|x|$};
\addplot[thick, blue, domain=-10.5:6.5, samples=100] {-abs(1/3*x+1)};
\addplot[only marks, mark=*, blue] coordinates {(-3,0) (-6,-1) (0,-1)};
\node[blue, font=\small] at (axis cs:-7,-3) {$m(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

  \item[(b)] Parent: $f(x) = x^2$.\\
  Transformations: (i) shift left 2 units, (ii) shrink vertically by a factor of $\dfrac{1}{3}$, (iii) reflect over the $x$-axis, (iv) shift up 3 units.

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-10.5, xmax=6.5, xtick={-10,-5,0,5},
  ymin=-7.5, ymax=10.5, ytick={-5,0,5,10},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, magenta, domain=-3.2:3.2, samples=60] {x^2};
\addplot[only marks, mark=*, magenta] coordinates {(0,0) (-3,9) (3,9)};
\node[magenta, font=\small] at (axis cs:4,6) {$f(x)=x^2$};
\addplot[thick, blue, domain=-7.5:3.5, samples=80] {-1/3*(x+2)^2+3};
\addplot[only marks, mark=*, blue] coordinates {(-2,3) (-5,0) (1,0)};
\node[blue, font=\small] at (axis cs:-6,2) {$g(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

\end{enumerate}
\end{solution}
\end{problem}


\begin{problem}
Given the parent function, apply the transformations in order to find $g(x)$.
\begin{enumerate}
  \item[(a)] $f(x) = \sqrt[3]{x}$: (i) shift 1 unit left, (ii) stretch horizontally by a factor of 4, (iii) reflect over the $x$-axis.
  \medskip
  \item[(b)] $f(x) = \dfrac{1}{x}$: (i) shift 2 units left, (ii) reflect over the $y$-axis, (iii) stretch vertically by a factor of 3, (iv) shift down 1 unit.
\end{enumerate}

\begin{solution}
\begin{enumerate}
  \item[(a)] Step by step:
  \begin{itemize}
    \item Shift left 1: $\sqrt[3]{x+1}$
    \item Stretch horizontally by 4: $\sqrt[3]{\dfrac{x}{4}+1}$
    \item Reflect over $x$-axis: $g(x) = -\sqrt[3]{\dfrac{x}{4}+1}$
  \end{itemize}

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=5cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-12.5, xmax=4.5, xtick={-12,-8,-4,0,4},
  ymin=-5.5, ymax=5.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-12.4:4.4, samples=200] {-((x/4+1)^(1/3))};
\addplot[only marks, mark=*, blue] coordinates {(-8,1) (-4,0) (0,-1)};
\node[blue, font=\small] at (axis cs:3,-3) {$g(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

  \item[(b)] Step by step:
  \begin{itemize}
    \item Shift left 2: $\dfrac{1}{x+2}$
    \item Reflect over $y$-axis: $\dfrac{1}{-x+2}$
    \item Stretch vertically by 3: $\dfrac{3}{-x+2}$
    \item Shift down 1: $g(x) = \dfrac{3}{-x+2} - 1$
  \end{itemize}

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-8.5, xmax=8.5, xtick={-8,-4,0,4,8},
  ymin=-8.5, ymax=8.5, ytick={-8,-4,0,4,8},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-8.5:1.67, samples=100] {3/(-x+2)-1};
\addplot[thick, blue, domain=2.4:8.5, samples=100] {3/(-x+2)-1};
\addplot[only marks, mark=*, blue] coordinates {(-1,0) (3,-4)};
\addplot[dashed, thin, blue] coordinates {(2,-8.5) (2,8.5)};
\addplot[dashed, thin, blue] coordinates {(-8.5,-1) (8.5,-1)};
\node[blue, font=\small] at (axis cs:5,5) {$g(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

\end{enumerate}
\end{solution}
\end{problem}


\begin{problem}
Given the graph of $f(x)$ below, sketch the graph of $g(x)$.
\begin{enumerate}
  \item[(a)] $g(x) = -f(x-1)+2$

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-5.5, xmax=5.5, xtick={-4,-2,0,2,4},
  ymin=-5.5, ymax=5.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-4:2, samples=80] {-1+sqrt(9-(x+1)^2)};
\addplot[thick, blue, domain=2:4] {-1};
\addplot[only marks, mark=*, mark options={fill=white}, blue] coordinates {(-4,-1)};
\addplot[only marks, mark=*, blue] coordinates {(4,-1)};
\node[blue, font=\small] at (axis cs:-3,3.5) {$f(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

  \medskip
  \item[(b)] $g(x) = -\dfrac{1}{2}f\!\left(\dfrac{1}{2}x\right)$

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-4.5, xmax=4.5, xtick={-4,-2,0,2,4},
  ymin=-4.5, ymax=4.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-2:0, samples=60] {4*(x+1)^2-4};
\addplot[thick, blue, domain=0:2, samples=60] {-4*(x-1)^2+4};
\addplot[only marks, mark=*, blue] coordinates {(-2,0) (2,0)};
\node[blue, font=\small] at (axis cs:-3,3) {$f(x)$};
\end{axis}
\end{tikzpicture}
\end{center}
\end{enumerate}

\begin{solution}
\begin{enumerate}
  \item[(a)] Shift $f$ right 1, reflect over the $x$-axis, then shift up 2:

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-5.5, xmax=5.5, xtick={-4,-2,0,2,4},
  ymin=-5.5, ymax=5.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, gray!50, domain=-4:2, samples=80] {-1+sqrt(9-(x+1)^2)};
\addplot[thick, gray!50, domain=2:4] {-1};
\addplot[thick, magenta, domain=-3:3, samples=80] {3-sqrt(9-x^2)};
\addplot[thick, magenta, domain=3:5] {3};
\addplot[only marks, mark=*, mark options={fill=white}, magenta] coordinates {(-3,3)};
\addplot[only marks, mark=*, magenta] coordinates {(5,3)};
\node[magenta, font=\small] at (axis cs:3.5,-2) {$g(x)$};
\node[gray, font=\small] at (axis cs:-3,3.5) {$f(x)$};
\end{axis}
\end{tikzpicture}
\end{center}

  \item[(b)] Stretch $f$ horizontally by 2, shrink vertically by $\frac{1}{2}$, and reflect over the $x$-axis:

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-4.5, xmax=4.5, xtick={-4,-2,0,2,4},
  ymin=-4.5, ymax=4.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, gray!50, domain=-2:0, samples=60] {4*(x+1)^2-4};
\addplot[thick, gray!50, domain=0:2, samples=60] {-4*(x-1)^2+4};
\addplot[thick, magenta, domain=-4:0, samples=60] {-0.5*(x+2)^2+2};
\addplot[thick, magenta, domain=0:4, samples=60] {0.5*(x-2)^2-2};
\addplot[only marks, mark=*, magenta] coordinates {(-4,0) (4,0)};
\node[magenta, font=\small] at (axis cs:2.5,2.5) {$g(x)$};
\node[gray, font=\small] at (axis cs:-3,3) {$f(x)$};
\end{axis}
\end{tikzpicture}
\end{center}
\end{enumerate}
\end{solution}
\end{problem}


\begin{problem}
Using transformations on a basic function, write a formula for $f(x)$ whose graph is shown below.
\begin{enumerate}
  \item[(a)]

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-4.5, xmax=4.5, xtick={-4,-2,0,2,4},
  ymin=-4.5, ymax=4.5, ytick={-4,-2,0,2,4},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-3:4.3, samples=60] {2*sqrt(x+3)-1};
\addplot[only marks, mark=*, blue] coordinates {(-3,-1) (-2,1) (1,3)};
\end{axis}
\end{tikzpicture}
\end{center}

  \medskip
  \item[(b)]

\begin{center}
\begin{tikzpicture}
\begin{axis}[
  axis lines=center, width=6cm, height=6cm,
  xlabel={$x$}, ylabel={$y$},
  xmin=-10.5, xmax=10.5, xtick={-10,-5,0,5,10},
  ymin=-10.5, ymax=10.5, ytick={-10,-5,0,5,10},
  grid=both, minor tick num=1, tick style={draw=none},
]
\addplot[thick, blue, domain=-10.3:3.55, samples=100] {5/(x-4)+1};
\addplot[thick, blue, domain=4.53:10.3, samples=100] {5/(x-4)+1};
\addplot[dashed, thin, blue] coordinates {(4,-10.5) (4,10.5)};
\addplot[dashed, thin, blue] coordinates {(-10.5,1) (10.5,1)};
\addplot[only marks, mark=*, blue] coordinates {(3,-4) (5,6)};
\end{axis}
\end{tikzpicture}
\end{center}
\end{enumerate}

\begin{solution}
\begin{enumerate}
  \item[(a)] Parent function $\sqrt{x}$: shift left 3, stretch vertically by 2, shift down 1. $\quad f(x) = 2\sqrt{x+3} - 1$

  \item[(b)] Parent function $\dfrac{1}{x}$: shift right 4 (VA at $x=4$), stretch vertically by 5, shift up 1 (HA at $y=1$). $\quad f(x) = \dfrac{5}{x-4} + 1$
\end{enumerate}
\end{solution}
\end{problem}

\end{document}
